// The route-level layout lives in src/layouts/DashboardLayout.jsx (it
// composes this folder's Sidebar/Topbar/MobileNav). This file re-exports
// it so imports from either "components/layout" or "layouts" resolve to
// the same component, instead of maintaining two copies.
export { default } from "../../layouts/DashboardLayout";
